# Production default — nginx proxies /api → Express:4000
# This file is loaded by Next.js during `next build` when NODE_ENV=production.
# Any real env var or .env.local override takes precedence.
NEXT_PUBLIC_API_URL=/api
