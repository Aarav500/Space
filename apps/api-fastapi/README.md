# FastAPI Backend (Placeholder)

> **Status:** Reserved for future use.

This folder is reserved for a future **FastAPI** (Python) backend template.

For **v1**, the active backend is `apps/api-node` (Node.js + Express).

## Planned Stack

- Python 3.11+
- FastAPI + Uvicorn
- SQLAlchemy + Alembic (Postgres)
- Pydantic for validation

## When to Use

Consider adding a FastAPI backend when you need:
- ML/AI model serving
- CPU-intensive data processing
- Python-specific library integrations

Until then, use `apps/api-node` for all backend needs.
